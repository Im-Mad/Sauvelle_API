package ma.sauvelle.models;

public enum Status {
    PENDING,
    CONFIRMED,
    PREPARED,
    SHIPPED,
    DELIVERED,
    PAID,
    OUT_OF_STOCK,
    UNREACHED,
    CANCELED,
    REFUSED,
    AWAITING_RETURN,
    RETURNED
}
